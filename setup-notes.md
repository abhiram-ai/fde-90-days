python --version              # Should be 3.11+
node --version                # Should be 20+
pnpm --version                # Should print a version
docker run hello-world        # Should print Docker's hello message
git config --global user.name # Should print your name
ssh -T git@github.com         # Should say "Hi <username>!"